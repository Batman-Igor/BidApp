spring:
  application:
    name: GeneralService

server:
  port: 8000

eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka